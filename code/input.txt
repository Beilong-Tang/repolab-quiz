from simple_judge.models import Questiondict
for q in Questiondict.objects.all():
    if q.question_title.find('chap02')!=-1 and q.question_title.find('sec-2.1')!=-1:
        q.question_week=2
        q.save()
